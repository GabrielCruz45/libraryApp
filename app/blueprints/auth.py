# This file will handle user authentication. 
# It will contain all the routes related to signing up, logging in, and 
# logging out (/register, /login, /logout). We'll define this as a Flask Blueprint.